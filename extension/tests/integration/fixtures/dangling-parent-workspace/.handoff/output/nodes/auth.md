---
id: auth
title: Auth
depth: supporting
schema_version: 1
parent: nonexistent-parent
---

## Business Context

Auth node whose parent does not exist in the index — used to verify graceful fallback to root-level.

## Technical Context

The parent field references a non-existent node id.
